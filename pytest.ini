[pytest]
# Only test the main project: unit tests at the root plus the functional tests.
testpaths = oups_test.py tests

# Never recurse into the vendored GitNexus project, the virtualenv, or VCS dirs.
norecursedirs = GitNexus .venv .git __pycache__ .pytest_cache .ruff_cache .ropeproject